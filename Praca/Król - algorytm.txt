#include <cstdlib>
#include <iostream>
#include <opencv2/opencv.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>

using namespace std;
using namespace cv;

int main(){
 Mat img = imread("./xxx", 1 );	//./xx - nazwa wczytywanego obrazu
	Mat kernel(3,3,CV_32F,cv::Scalar(0));	//rozmiar macierzy teorzacej baze filtra
					//nalezy teraz ustaliæ wartoœci macierzy dla tworzonego filtra
	
	namedWindow( "Sharpen", WINDOW_AUTOSIZE );
  imshow( "Sharpen", img );
  waitKey(0);
  return 0;  
}